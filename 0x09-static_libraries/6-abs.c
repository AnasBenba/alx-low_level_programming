#include "main.h"

/**
* _abs -> computes the absolute value
* Return: return positive or a negative number
* @n: number to check
*/
int _abs(int n)
{
	if (n < 0)
	{
		n = n * -1;
		return (n);
	}
	else
	{
		return (n);
	}
}
