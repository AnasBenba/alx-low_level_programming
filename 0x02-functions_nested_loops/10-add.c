#include "main.h"

/**
* add -> check for the sum of two numbers
* Return: return the sum of two numbers
* @n: number to add
* @s: number to add
*/
int add(int n, int s)
{
	int sum = n + s;

	return (sum);
}
